filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/django/*.log
    - /var/log/postgresql/*.log
    - /var/log/nginx/*.log

processors:
  - add_cloud_metadata: ~

output.logstash:
  hosts: ["logstash:5044"]
